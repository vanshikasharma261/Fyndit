/**
 * Auth-module constants shared between the strategy, guard and service.
 */

/** Name of the HTTP-only cookie that carries the access token. */
export const ACCESS_TOKEN_COOKIE = 'access_token';

/** Passport strategy identifier registered by {@link JwtStrategy}. */
export const JWT_STRATEGY = 'jwt';
